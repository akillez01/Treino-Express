"""Ponto de extensão da Fase 3: Stripe Connect (docs/06-stripe-connect.md).

Vai cobrir: onboarding de conta conectada por academia (Express), criação de
PaymentIntent para recarga de crédito do anunciante (`POST /anunciante/creditos`),
split automático via `transfer_data` e webhooks de conciliação diária. Nada
implementado ainda nesta fase — só a dependência `stripe` já está instalada.
"""
